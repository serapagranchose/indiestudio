extern "C" {
    #include <raylib.h>
}

#include <ostream>
#include <iostream>

int main(int argc, char * argv[]) {
    std::cout << "! dlrow olleH" << std::endl;
    Color textColor = LIGHTGRAY;
    return (0);
}
